# CLAUDE.md — DocuGuard Project Context

> Persistent context for Claude Code across sessions. Update the **Build Progress** and
> **Decisions** sections as work advances. Read this first when resuming work.

## What this is
**DocuGuard** — a full-stack AI-powered Document Intelligence & Compliance Automation system.
Users upload documents (PDF/DOCX/images); the system OCRs + extracts text, classifies the
document, validates it against compliance frameworks (HIPAA, GDPR, SOC 2, PCI-DSS), detects
violations, calls AWS Bedrock (Claude 3.5 Sonnet) for fix suggestions + summaries, calculates a
risk score, and produces downloadable audit reports. A role-based dashboard drives upload →
review → approve/reject → report.

The authoritative spec is **`PROMPT (1).md`** at the repo root. The phased build plan is in
**`DocuGuard-Development-Plan.docx`** (generated by `generate-plan.js`).

## Hard constraints (from the spec — do not violate)
- **Storage = local filesystem** under `LOCAL_STORAGE_DIR` (`./storage`), served only through
  **authenticated download endpoints**. **No S3, no cloud object storage.** Textract takes local
  file bytes directly. (An `-AsIs-S3` docx variant exists for reference only — it is NOT the design.)
- **Auth = custom JWT** (python-jose) + **bcrypt** (cost 12). No Firebase, no OpenAI, no external auth.
- **AI = AWS Bedrock only** (Claude 3.5 Sonnet). Every Bedrock call wrapped in try/except with a
  `generic_fix` fallback.
- **OCR = Tesseract (local) → AWS Textract (fallback).**
- Access token in **memory only** (never localStorage); refresh token in **httpOnly cookie**.
- `extracted_text` is **AES-256 Fernet encrypted** before storing in MongoDB.
- Motor (async) for all MongoDB access — never PyMongo directly, no raw string queries.
- Celery updates the document `status` field after every pipeline step.

## Tech stack
- **Frontend:** React (Vite), Tailwind, React Router v6, Axios (JWT refresh interceptor),
  React Dropzone, @react-pdf-viewer/core + pdfjs-dist, React Toastify, Framer Motion.
- **Backend:** FastAPI, Celery + Redis, Motor, Apache Tika, pytesseract + Pillow, python-magic,
  spaCy `en_core_web_sm`, boto3, python-jose, passlib[bcrypt], reportlab, python-docx, slowapi,
  cryptography (Fernet), aiofiles.
- **Data:** MongoDB (Atlas in prod; local `mongo` container for dev), Redis (Celery broker + JWT blacklist).
- **Cloud:** AWS Textract + Bedrock (one IAM user). Local filesystem for files.

## Repo layout (this repo root == project root)
```
backend/   FastAPI app (app/config, database, dependencies, models, routers, services, tasks, utils, scripts)
frontend/  React + Vite app (src/api, context, hooks, components, pages)
storage/   Local file storage (gitignored) — documents/ and reports/
docker-compose.yml   api + worker + redis + mongo + frontend
PROMPT (1).md        Authoritative spec
generate-plan.js     DOCX dev-plan generator (STORAGE=local default | STORAGE=s3 variant)
```

## How to run (one command)
```bash
cp backend/.env.example backend/.env   # then fill AWS keys + generate secrets
docker compose up --build
# api    -> http://localhost:8000  (Swagger at /docs)
# front  -> http://localhost:5173
```
Generate required secrets:
```bash
# JWT secret (32+ chars)
python -c "import secrets; print(secrets.token_urlsafe(48))"
# Fernet field-encryption key
python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
```
Seed the first admin (after stack is up):
```bash
docker compose exec api python -m app.scripts.seed_admin --email admin@docuguard.local --password ChangeMe123!
```

## The five phases (gate-driven — no phase starts until prior QA passes)
1. **Foundation** — infra/Docker, custom JWT auth, local storage, MongoDB schemas, Celery. ← current
2. **Processing Pipeline** — upload, OCR (Tesseract→Textract), text extraction, classification.
3. **Compliance Engine & AI** — rule engine, 4 checklists, Bedrock summaries + fix suggestions, risk score.
4. **Dashboard & Reports** — full React UI, PDF viewer w/ violation highlights, audit reports, notifications.
5. **Security & Hardening** — RBAC end-to-end, encryption, rate limiting, headers/CORS, pen-test readiness.

## Build Progress
- [x] Phase 0 — Development plan document generated (local + S3 variants).
- [x] **Phase 1 — Foundation (VERIFIED LIVE ✅)**
  - Backend: config, database (Motor + Redis), JWT auth (register/login/refresh/logout/me),
    bcrypt hashing, Fernet encryption util, storage_service (local), document upload endpoint,
    MongoDB schemas + indexes, Celery app + chained task skeleton, /api/health, seed-admin script.
  - Frontend: Vite + Tailwind, AuthContext (memory access token + refresh-on-load), Axios
    interceptor (401 → refresh → retry), Login/Register/Dashboard, protected routes.
  - Infra: docker-compose (api, worker, redis, mongo, frontend), backend Dockerfile, README.
  - Access tokens carry a unique `jti` (each issue distinct/revocable).
- [x] **Phase 2 — Document Processing Pipeline (VERIFIED LIVE, non-AWS paths ✅)**
  - Services: `bedrock_service` (call_bedrock, summarize_document, get_ai_fix_suggestion,
    classify_document_with_ai — all try/except), `parser_service` (Tika PDF + python-docx),
    `ocr_service` (Tesseract→Textract for images, Tika→Textract for PDF, local bytes),
    `classifier_service` (9-type rule scoring → Bedrock fallback @ <0.75), `nlp_service` (spaCy NER).
  - Celery: real `extract_text` (OCR → **Fernet-encrypt** `extracted_text` → store) and
    `classify_document` (decrypt → classify → store), fail-soft (`status=failed`) + None-guards.
  - File validation: content-first magic bytes + anti-spoof (extension must agree with content).

### Verification (last run — stack live via `docker compose up`)
- **Offline service QA** (`docker compose exec -T worker python -m tests.test_offline`): **9/9**
  (DOCX/PNG-Tesseract/PDF-Tika extraction, rule classification, Fernet round-trip, storage + traversal block).
- **HTTP QA** (`python scripts/qa_http.py`): **16/16** — full auth flow incl. Redis blacklist,
  415 on `.exe` + PNG-spoofed-as-`.pdf`, 401 unauth upload, DOCX upload → pipeline → `completed`,
  classified `Legal Contract`.
- Direct checks: `extracted_text` stored as `gAAA…` ciphertext in MongoDB; Celery `ping → pong`.
- **NOT yet exercised:** Textract + Bedrock AI fallbacks (need real AWS creds in `backend/.env`;
  current values are placeholders). Re-run QA after adding creds to cover the AI paths.
- [x] **Phase 3 — Compliance Engine & AI (VERIFIED LIVE, non-AWS paths ✅)**
  - Checklists: `hipaa`(12), `gdpr`(12), `soc2`(10), `pci_dss`(10), `internal_audit`(8) JSON files.
  - `compliance_engine.py` — pattern / keyword_required / keyword_forbidden / nlp_entity detection,
    precise char offsets, risk score (critical 40 / warning 20 / info 5, capped 100), Low/Med/High.
  - `compliance` router — checklist CRUD (built-ins read-only; create=admin/reviewer, delete=admin)
    + `POST /validate/:id` re-trigger (optional checklist switch).
  - Celery: real `run_compliance` (decrypt → evaluate → Bedrock `get_ai_fix_suggestion` per
    violation → store violations+risk), `generate_summary` (Bedrock `summarize_document`),
    `notify_reviewers` (insert notifications for reviewers/admins). `start_revalidation` chain added.
  - `GET /documents/:id` now returns `DocumentDetail` (violations, summary, risk, etc.).
  - Verified: engine QA **17/17** (`tests/test_compliance.py`), HTTP QA **14/14** (`scripts/qa_phase3.py`)
    — SSN→HIPAA-001 critical with exact offsets, risk High/100, ai_fix_suggestion populated
    (fell back to "Manual review required." with placeholder AWS creds), checklist RBAC enforced.
  - **NOT yet exercised:** real Bedrock summaries/fix text (needs AWS creds).
- [ ] Phase 4–5 — not started. Phase 4 = full React dashboard, PDF viewer + violation highlights,
  audit reports (PDF/DOCX), notifications. **Use the .agents frontend skills (frontend-design,
  impeccable, design-motion-principles) when building Phase 4 UI.**

## Conventions
- Roles: `admin | reviewer | auditor`. Default on register = `reviewer`. RBAC via `require_role(*roles)`.
- Document status lifecycle: `pending → extracting → classifying → validating → completed | failed`.
- Approval status: `pending → approved | rejected`.
- All write actions append to `audit_logs`. Notifications in `notifications` collection.
- MongoDB ObjectIds serialized to `str` in API responses; helper in `app/utils/helpers.py`.
- API base path: `/api`. Frontend reads `VITE_API_BASE_URL`.

## Decisions / deviations (with rationale)
- **Added a `mongo` container to docker-compose** (spec lists only Atlas). Rationale: Phase 1
  objective is "run the stack locally in one command"; requiring an Atlas signup breaks that.
  `MONGODB_URI` still points at Atlas in production by editing `.env`.
- Kept everything else exactly to spec.

## Gotchas
- `passlib[bcrypt]==1.7.4` breaks with `bcrypt>=4.1` → pinned `bcrypt==4.0.1`.
- `python-magic` needs `libmagic1` (installed in the backend Dockerfile) — won't import on bare Windows; run via Docker.
- Tika needs Java (`default-jdk` in Dockerfile); spaCy model downloaded at image build time.
  Tika also fetches its server jar from Maven on first use (one-time network, not AWS).
- `EmailStr`/email-validator **rejects reserved domains** like `*.local` → use real-looking
  domains (e.g. `@example.com`) for API registration and tests. (`seed_admin` inserts directly
  into Mongo, bypassing this validation, so an `admin@docuguard.local` seed is fine.)
- QA harnesses: `backend/tests/test_offline.py` (in-container, no AWS) and `scripts/qa_http.py`
  (host, stdlib-only). Run the offline one first — it generates the DOCX fixture the HTTP one uploads.
